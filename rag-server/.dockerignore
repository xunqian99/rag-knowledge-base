# target 是本地构建产物,镜像里会重新构建,没必要拷进去
target/

# 【最重要的一条】本地私有配置里有 API 密钥。
# 虽然容器最终是通过只读挂载拿到这个文件的,但把它排除出构建上下文
# 能彻底杜绝「某次构建不小心把密钥打进镜像层」的可能。
application-local.yml

# IDE 与编辑器文件
.idea/
*.iml
.vscode/

# 本地数据
uploads/
